# Pseudo Agent Skills

Antigravity で Agent Skills もどきができるかテスト。

## 環境構築

```
uv sync
```

## 実験

以下のプロンプトを送ると、`make_dummy.py` を使用してダミーの人名を 10 個生成して返す。

```
人名を10個作ってください。
```

以下のプロンプトを送ると、LLM が自身で生成して返す。

```
犬の名前を10個作ってください。
```
